spring:
  application:
    name: backend
  security:
    user:
      name: user  # Placeholder for a more secure authentication mechanism (see note below)
      password: 1111  # Replace with encrypted password using Spring Security
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/everycaredb
    username: everycareroot
    password: Everycare2024!
  jpa:
    hibernate:
      ddl-auto: update
  http:
      encoding:
      charset: UTF-8
      enabled: true
      force: true


springdoc:
  swagger-ui:
    groups-order: DESC
    tags-sorter: alpha
    operations-sorter: method
    disable-swagger-default-url: true
    display-request-duration: true
    defaultModelsExpandDepth: 2
    defaultModelExpandDepth: 2
  api-docs:
    path: /api-docs
  show-actuator: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  writer-with-default-pretty-printer: true
  model-and-view-allowed: true
  paths-to-match:
    - /api/v1/**
